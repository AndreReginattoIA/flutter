import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:slidy_modular/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ProductController product;
  //
  setUp(() {
    //     product = AppModule.to.get<ProductController>();
  });

  group('ProductController Test', () {
    //   test("First Test", () {
    //     expect(product, isInstanceOf<ProductController>());
    //   });

    //   test("Set Value", () {
    //     expect(product.value, equals(0));
    //     product.increment();
    //     expect(product.value, equals(1));
    //   });
  });
}
