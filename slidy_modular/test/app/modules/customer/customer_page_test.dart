import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:slidy_modular/app/modules/customer/customer_page.dart';

main() {
  testWidgets('CustomerPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CustomerPage(title: 'Customer')));
    //  final titleFinder = find.text('Customer');
    //  expect(titleFinder, findsOneWidget);
  });
}
