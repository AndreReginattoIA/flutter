import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:slidy_modular/app/modules/order/order_controller.dart';
import 'package:slidy_modular/app/modules/order/order_module.dart';

void main() {
  initModule(OrderModule());
  // OrderController order;
  //
  setUp(() {
    //     order = OrderModule.to.get<OrderController>();
  });

  group('OrderController Test', () {
    //   test("First Test", () {
    //     expect(order, isInstanceOf<OrderController>());
    //   });

    //   test("Set Value", () {
    //     expect(order.value, equals(0));
    //     order.increment();
    //     expect(order.value, equals(1));
    //   });
  });
}
