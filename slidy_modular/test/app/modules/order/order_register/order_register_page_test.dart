import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:slidy_modular/app/modules/order/order_register/order_register_page.dart';

main() {
  testWidgets('OrderRegisterPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(OrderRegisterPage(title: 'OrderRegister')));
    //  final titleFinder = find.text('OrderRegister');
    //  expect(titleFinder, findsOneWidget);
  });
}
