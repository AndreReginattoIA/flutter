import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:slidy_modular/app/modules/order/order_page.dart';

main() {
  testWidgets('OrderPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(OrderPage(title: 'Order')));
    //  final titleFinder = find.text('Order');
    //  expect(titleFinder, findsOneWidget);
  });
}
